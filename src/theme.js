import { createMuiTheme }  from '@material-ui/core/styles'

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#dfcc26',
    },
    secondary: {
      main: '#dfcc26',
    },
  },
  text: {
    primary: "#fff",
    secondary: "#fff",
  }
});

export default theme;